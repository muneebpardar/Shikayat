namespace Shikayat.Domain.Enums
{
    public enum LocationType { Province = 1, District = 2, Tehsil = 3 }
    public enum ComplaintStatus { Pending, InProgress, Resolved, Rejected }
    public enum ComplaintPriority { Normal, High, Critical }
}